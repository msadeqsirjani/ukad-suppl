trainer: !include "../../trainers/gpu.yaml"
model:
  module: src.models
  class_name: UltrasoundSegmentationModel
  config:
    network: !include "../../nets/ufunkan_seg_512.yaml"
    criterion: !include "../../losses/composite.yaml"
    optimizer: !include "../../optimizers/adam_seg.yaml"
data:
  train_dataloaders:
  - !include "../../data/glas/train_dataloader.yaml"
  val_dataloaders:
  - !include "../../data/glas/val_dataloader.yaml"
